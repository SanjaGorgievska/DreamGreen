import React from 'react'
import moment from 'moment'

const Notifications = (props) => {
  const { notifications } = props;
  return (
    <div className="section" id="notifikacija">
     
      </div>
   
  )
}

export default Notifications